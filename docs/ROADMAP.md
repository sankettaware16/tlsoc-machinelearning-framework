# Roadmap

The spec's own phasing (SPEC_DIGEST §12) assumes a funded team and a live
enterprise estate. This roadmap re-sequences the same content for how the project
is actually being built: **offline-first, vertical slices, each phase ending in
something runnable and measurable.**

Guiding rule: **never build a layer that cannot be exercised end-to-end.** A
half-built feature platform with no use case on top of it is unverifiable work.
So the first milestone deliberately drives one use case all the way from log file
to explained alert, on a thin version of every layer, and later phases widen
rather than deepen.

Phases are ordered by dependency, not calendar-dated.

---

## Phase 0 — Foundations ✅ *complete*

**Exit:** the skeleton exists and the contract is proven against real sample logs.

- [x] Read reference material; distil spec into `SPEC_DIGEST.md`
- [x] Architecture, requirements, brief, research
- [x] Modular scaffold + six plugin interfaces
- [x] Contributor documentation (`docs/DEVELOPING.md`, invariants, module map)
- [x] `core/`: Event contract + validation, config loader, plugin registry
- [x] `ingest/file`: file-tail source — checkpoints, replay/rewind, partial-line
      safety, rotation detection, DLQ (FR-02, FR-04, NFR-09)
- [x] `preprocess/`: sessionization + the spec's session feature set (FR-07)
- [x] Contract tests against `log_samples/*.json` (all four sources)
- [x] `soc-ml validate` — contract + field coverage + **timestamp quality** (FR-08)
- [x] `soc-ml sessions` — visit-shape report, shows whether data can support
      per-session features at all
- [x] `soc-ml lint-config` — proves config carries policy, not thresholds (FR-62)

**51 tests passing.** Nothing here needs Kafka, Redis, or Elasticsearch.

**Data note:** `log_samples/nginx_sample.json` was stale output from a superseded
engine version (ingest-time `@timestamp`). It has been regenerated by replaying
its raw lines through the current engine — event times are now parser-attested
(`timestamp_source: log`), and median inter-arrival CV moved from 0.00 to 3.41.
The old output is retained as an FR-08 regression fixture. Both nginx and
Moodle/Apache are now sound Phase 1 fixtures. See `JOURNAL.md` D-014.

---

## Phase 1 — First vertical slice: web_recon (UC-02) offline ✅ *complete*

**Exit met:** `soc-ml backtest --uc web_recon --input <logs>` produces scored,
explained findings with a metrics report — no Kafka, Redis, or ES.

- [x] `docs/NAMING.md`: slug + spec-ID + title naming standard, full catalog,
      CI-enforced (`tests/test_naming.py`) — D-016
- [x] `baseline/`: Environment Profile v1 — path IDF, UA frequency, served
      extensions, population feature stats (p50/p99); JSON-persisted artifact
- [x] `features/`: streaming tumbling-window builder (5m per entity, bounded
      memory, watermark close, evidence retention) — D-017
- [x] `models/`: `isolation_forest` + `lof_novelty` wrappers, uniform
      fit/score/save/load, deterministic fits
- [x] `usecases/web_recon.py`: spec features + max-fusion + gate
      (fused pct ≥ 99.7, evidence floor ≥5 events / ≥3 paths)
- [x] `fusion/`: 1001-point percentile calibration + the severity formula
- [x] `explain/`: population-context attributions + deterministic narrative +
      verbatim evidence lines
- [x] `alerting/file` sink (ECS-aligned, naming triple in every alert)
- [x] `evaluation/`: backtest harness — chronological split, **corpus hygiene
      (p99.9 clip + top-0.1% drop, FR-56)**, deterministic **canary** injected
      into scoring only (FR-58), every score recorded
- [x] `soc-ml backtest` CLI (fails the run if the canary is missed)
- [x] 92 tests passing; e2e test gates on canary detection + FP budget

**Thesis proven on real data:** on 300k events of live production nginx traffic the
slice detected a genuine directory-enumeration attacker (203.0.113.199, ~150
req/5min, ~95 never-served paths, ~100×404) plus the canary — zero literal
thresholds anywhere. Deferred within-phase: generic `StateStore` impl (build
when the live loop needs it), PR-AUC (needs SAIF ground truth, Phase 3),
`orjson` ingest speedup (D-017).

**Carried forward:** raw alert volume on the real run (9 alerts/2h, 6 from the
one attacker) is the alert-storm shape that Phase 3's campaign folding and
progressive alerting exist to collapse — see D-017.

---

## Phase 2 — Learning lifecycle + production runtime ✅ *core complete*

**Exit met:** `web_recon` is deployable as a service — trains, versions, serves,
tails live traffic, checkpoints, self-monitors, notices drift, and never floods.
Full train→promote→run cycle validated on 400k live nginx events.

- [x] `registry/`: versioned filesystem store, 3 hot versions, atomic `current`
      pointer, candidate→promote, rollback, metadata (window, rows,
      feature-code SHA)
- [x] `training/trainer.py`: shared trainer + corpus hygiene (p99.9 clip, top
      0.1% drop) — used by backtest AND `soc-ml train`
- [x] `detection/scorer.py`: shared scoring — backtest and live run identical
      code (FR-72)
- [x] `detection/runtime.py`: live loop — tail → window → score → dedup → budget
      → sink, checkpoint+resume, health JSON, graceful SIGTERM drain, cold-start
- [x] `detection/dedup.py` + `detection/budget.py`: per-entity fold + daily
      per-server budget → digest (FR-34)
- [x] `drift/psi.py`: PSI vs training reference, hourly, retrain trigger (FR-51)
- [x] Modes observe/shadow/live, per-use-case (staged cold-start)
- [x] `soc-ml train` / `run` / `promote` / `status` (+ `--rollback`)
- [x] `DEPLOYMENT.md` + systemd unit + config runtime knobs

**Deferred (Phase 2 remainder, needed at multi-use-case scale, not for single-UC
deploy):** cadence-ladder *scheduler* (cron/timer suffices now); KS/ADWIN
(PSI covers the trigger); the ≥72 h automated challenger soak (manual promote is
the gate today); incident-window quarantine (no confirmed-incident store yet).

**Known gap carried to Phase 3:** single-detector alert volume exceeds the
≤3/day budget until false-positive suppression (UC-04 crawlers, UC-12 campaigns,
corroboration) lands. The daily budget bounds it in the meantime — see D-018.

---

## Phase 3 — bot_detection (UC-04) + cross-use-case suppression *(next)*

**Why now:** the `web_recon` production shadow run proved D-008 — search-engine
crawlers are its dominant false-positive source. `bot_detection` is the fix, and
it is the first test of the framework's core promise: *adding a detector is
repeating the proven web_recon path.* Design in `JOURNAL.md` D-019.

**Exit:** both detectors run in one live runtime; `web_recon`'s crawler fires
drop measurably because `bot_detection` suppresses verified/known crawlers — and
every suppression is visible in the alert document, never silent.

This phase has sub-steps because UC-04 introduces **cross-use-case signal
sharing**, which `web_recon` (a single self-contained detector) did not need.

### 3.0 — Multi-use-case plumbing *(done)*
- [x] Resolve any registered use case by slug in `train` / `backtest` / `run`
      (replace the `web_recon`-only maps with the plugin registry)
- [x] Runtime scores **N enabled use cases per window in dependency order**
      (UC-04 before UC-02), each with its own bundle, dedup, and budget
      (`UseCase.depends_on` + `usecases.dependency_order`)
- [x] Per-use-case health/shadow/alert files keyed by slug; the shared ingest
      checkpoint is keyed by the *set* of use cases (sorted slugs joined by
      `+`), so a single-use-case deployment resumes seamlessly after upgrade
- [x] Per-use-case backtest canary hook (`UseCase.canary`) — no canary yet
      means the check is skipped, never spuriously failed

### 3.1 — bot_detection features + labels *(done)*
- [x] `features/bot_features.py`: `bot.asset_fetch_ratio`,
      `bot.activity_hour_entropy`, `timing.fano_factor`,
      `bot.referrer_chain_depth`, `bot.path_repeat_ratio`,
      `bot.method_get_ratio`, `bot.bytes_per_req_p50`, `bot.robots_txt_fetched`;
      reuse `timing.interarrival_cv`, `ua.rarity`, `ua.len`, `web.*`.
      Cross-window inputs (hour entropy, robots.txt) come from a bounded LRU
      per-entity memory in the window builder
- [x] `bot.declared_bot` label from the UA string (keyword matcher) — the free
      self-supervised label; rides in the vector as the GBM target
- [x] Verified-crawler check (Googlebot/Bingbot published ranges) — the spec's
      "free precision" identity allowlist (not a data threshold, FR-62 safe);
      carried in window *evidence*, never as a model feature

### 3.2 — bot_detection models *(done)*
- [x] `models/gbm.py` — gradient-boosted classifier: predict `declared_bot`
      from **behavior only** (label + `ua.*` structurally excluded — leakage),
      isotonic-calibrated → P(bot | behavior); degrades to raw probabilities /
      class prevalence when the minority class is too thin
- [x] `models/gmm.py` — BIC-selected GMM; bot-likeness = soft component
      membership weighted by each component's declared-bot share
      (human-likeness = 1 − score)
- [x] `models/hdbscan_cluster.py` — known-crawler clustering (optional `cluster`
      extra; `Model.available()` + the shared factory helper skip it with a
      warning when `hdbscan` is absent, NFR-08)
- [x] all with `score_batch` (perf) + `save`/`load`; hygiene clipping now
      skips binary features so a rare label can't be zeroed by p99.9

### 3.3 — bot_detection use case + gate *(done)*
- [x] `usecases/bot_detection.py` (class `BotDetection`, slug `bot_detection`,
      UC-04) — features + models + gate; cluster model excluded from gate
      fusion (association is export evidence, not spoofing evidence)
- [x] Gate: **browser-declared** entity with `P(bot|behavior) ≥ p99.5` sustained
      ≥30 min (6 consecutive 5m windows, event-time streaks) → UA-spoofing
      alert; declared bots never self-alert. Gate models calibrate against
      browser-declared windows (D-021). Spoofer canary
      (browser UA, machine behavior, 45 min) ships with the use case

### 3.4 — Cross-use-case signal export (the payoff) *(done)*
- [x] Shared per-entity annotation store (`detection/annotations.py`, bounded
      LRU): `crawler.human_likeness`, `crawler.is_known`, `crawler.is_verified`,
      each stamped with when and by which use case it was written
- [x] `bot_detection` writes it each window (new `UseCase.annotate` hook; raw
      isotonic P(bot|behavior) — the calibrated probability *is* the signal);
      the scorer injects the entity's current annotations into window evidence
      before gating, so later use cases read them (`entity_annotations`)

### 3.5 — web_recon consumes suppression *(done)*
- [x] `web_recon` consumes the crawler annotation → **suppress verified polite
      crawlers** (published-range identity + robots.txt fetched), **down-weight
      borderline** one severity band — recorded visibly on the alert document,
      in the shadow/score log, and in a per-slug `*_suppressed.ndjson` (D-022)
- [x] No change to `web_recon`'s own thresholds (still learned); the gate still
      fires and is recorded — suppression happens at the fusion/delivery layer
      (`UseCase.suppression`), a new *input*, not a new threshold

### 3.6 — Validate on elkcc
- [ ] Train + shadow both detectors on live nginx; measure `web_recon` fire-rate
      before/after suppression (Googlebot/Bingbot should stop firing)
- [ ] Confirm `bot_detection` flags a genuine UA-spoofer if present
- [ ] Decision gate: is the combined fire rate now within budget → go `--mode live`?

**After this phase**, UC-01/06/07 follow the same pattern; then the full fusion
layer (corroboration, campaign folding, fleet-simultaneity) and SAIF v1.

---

## Phase 3b — Remaining Tier-1 detectors

- [ ] UC-01 `credential_stuffing` — Half-Space Trees streaming + coordination layer
- [ ] UC-06 `content_scraping` — session autoencoder 16-8-4-8-16 (consumes UC-04 human-likeness)
- [ ] UC-07 `http_exfiltration` — path-family GMM + quantile forecaster + β-VAE drip
- [ ] Full fusion: corroboration, fleet-simultaneity suppression, campaign folding
- [ ] SAIF v1: per-use-case attack generators + regression gating

---

## Phase 4 — Analytics pipeline

**Exit:** a second, isolated runtime producing insight documents, and AU-11
feeding the fusion suppressor.

- [ ] `analytics/` runtime (separate process — NFR-13)
- [ ] AU-01 baselines/forecasts (STL + quantile GBM), AU-02 seasonal decomposition
- [ ] AU-11 operational anomalies → the fleet-simultaneity suppressor
- [ ] AU-03 geo drift, AU-05 resource popularity
- [ ] Insight sink + retention

**Why after Tier-1:** AU-01/02 supply time-of-week normalization that improves
detection, and AU-11 closes the loop on operational false positives.

---

## Phase 5 — Tier-2 and Tier-3 detection

**Exit:** the full use-case catalog.

- [ ] Tier-2: UC-03, UC-05, UC-08 (Lomb-Scargle + BOCPD), UC-09 (char LM cheap
      tier first, conv-AE behind a switch), UC-10 (HMM), UC-12 (campaign clustering)
- [ ] Tier-3: UC-11, UC-13 (chain HMM), UC-14 (novelty ensemble), UC-15 (endpoint roles)
- [ ] Remaining analytics AU-04, AU-06…AU-10, AU-12
- [ ] Semi-supervised feedback re-rankers (arm at ≥200 verdicts)

Ship UC-09's **4-gram LM tier first** — the spec notes it delivers ~80% of the
value at 5% of the cost, and it avoids making PyTorch a practical requirement.

---

## Phase 6 — Scale-out profiles

**Exit:** `cluster` and `enterprise` profiles work, with `standalone` unchanged.

- [ ] `ingest/kafka`, `state/redis`, `alerting/elasticsearch`, `alerting/kafka`
- [ ] MLflow registry adapter
- [ ] Multi-process / worker sharding; backpressure (shed Tier-3, protect Tier-1)
- [ ] ES index templates + ILM policies as code
- [ ] Prometheus metrics + health events

**Deliberately last.** Building the distributed path early would let it dictate
the design and quietly break NFR-07 (zero-infra default).

---

## Phase 7 — Hardening and release

- [ ] Red-team exercise → permanent gold set
- [ ] Backtest library over confirmed historical incidents
- [ ] State-loss and failover drills
- [ ] Quarterly governance review process (human naming of learned structures)
- [ ] Packaging, install docs, plugin authoring guide, public release

---

## Cross-cutting, from Phase 0 onward

Not a phase — these run continuously and are release-blocking at every phase.

- **The threshold lint** (FR-62). It must exist before use cases do, or literal
  thresholds will creep in and G1 dies quietly.
- **Contract tests** against the sample logs.
- **`benign-true-positive`** as a first-class verdict from the first alert schema.
- **Journal discipline** — every non-obvious decision recorded in `JOURNAL.md`.

---

## Sequencing risks

| Risk | Mitigation |
|---|---|
| Building the feature platform "properly" before any use case exists | Phase 1 is a vertical slice; the platform grows to fit real demand |
| UC-04 built late, so UC-02/06 drown in crawler false positives | UC-04 promoted to build-2nd in Phase 3 |
| Lifecycle bolted on after use cases | Phase 2 precedes the bulk of the catalog |
| Enterprise stack assumptions leaking into the core | Phase 6 last; NFR-07 tested on a fresh install |
| PyTorch becoming a hard dependency | cheap tiers first (4-gram LM), deep models behind extras + config switch |
| Cold start (2–8 weeks) discovered at deployment time | staged protocol built in Phase 2 and exercised offline via replay |
